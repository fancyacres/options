namespace Options.Core
{
    /// <summary>
    ///     Interface for types containing only non-white-space string values
    /// </summary>
    public interface INonWhiteSpaceOption : IOption<string>
    {
    }
}